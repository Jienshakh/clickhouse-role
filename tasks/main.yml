---
- name: Download, install and configure clickhouse
  tags: clickhouse
  block:
    - name: Download clickhouse
      block:
        - name: Get clickhouse distrib
          ansible.builtin.get_url:
            url: "https://packages.clickhouse.com/rpm/stable/{{ item }}-{{ clickhouse_version }}.noarch.rpm"
            dest: "./{{ item }}-{{ clickhouse_version }}.rpm"
            mode: '0644'
          with_items: "{{ clickhouse_packages }}"
      rescue:
        - name: Get clickhouse distrib
          ansible.builtin.get_url:
            url: "https://packages.clickhouse.com/rpm/stable/clickhouse-common-static-{{ clickhouse_version }}.x86_64.rpm"
            dest: "./clickhouse-common-static-{{ clickhouse_version }}.rpm"
            mode: '0644'
    - name: Install clickhouse packages
      become: true
      ansible.builtin.dnf:
        name:
          - clickhouse-common-static-{{ clickhouse_version }}.rpm
          - clickhouse-client-{{ clickhouse_version }}.rpm
          - clickhouse-server-{{ clickhouse_version }}.rpm
        disable_gpg_check: true
      notify: Start (restart) clickhouse service
    - name: Flush handlers
      ansible.builtin.meta: flush_handlers
    - name: Wait for ClickHouse TCP port
      ansible.builtin.wait_for:
        port: 9000
        delay: 5
        timeout: 60
    - name: Create database
      ansible.builtin.command: "clickhouse-client -q 'create database logs;'"
      register: create_db
      failed_when: create_db.rc != 0 and create_db.rc != 82
      changed_when: create_db.rc == 0
    - name: Create table
      ansible.builtin.command: >
        clickhouse-client -q
        "CREATE TABLE IF NOT EXISTS logs.logs
        (message String) ENGINE = MergeTree()
        ORDER BY tuple();"
      register: create_table
      changed_when: false
    - name: Enable ClickHouse external access
      become: true
      ansible.builtin.blockinfile:
        path: /etc/clickhouse-server/config.xml
        block: |
          <listen_host>::1</listen_host>
          <listen_host>0.0.0.0</listen_host>
        insertafter: '<http_port>8123</http_port>'
      notify: Start (restart) clickhouse service
